# AGENTS.md

## 交接文档约定

每个工作阶段结束时，写一份交接文档放入 `handoff/`，文件名格式：

```
handoff-<date>-<theme>.md
```

- `date`：`YYYY-MM-DD`，文档撰写日期
- `theme`：英文小写短横线，概括本阶段主题（如 `clay-visual`、`p0-p2-wrapup`）

新一轮工作开始前，先读 `handoff/` 里日期最新的一份恢复上下文。
